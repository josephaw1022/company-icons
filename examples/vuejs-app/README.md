# Vue Example App

## Run (macOS/Linux)
```bash
export NPM_TOKEN="$(gh auth token)"
npm install
npm run dev
````

## Run (Windows PowerShell)

```powershell
$env:NPM_TOKEN = (gh auth token)
npm install
npm run dev
```