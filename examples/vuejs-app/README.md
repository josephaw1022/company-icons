# Vue Example App

## Run (macOS/Linux)
```bash
export NODE_AUTH_TOKEN="$(gh auth token)"
npm install
npm run dev
````

## Run (Windows PowerShell)

```powershell
$env:NODE_AUTH_TOKEN = (gh auth token)
npm install
npm run dev
```